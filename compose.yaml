services:
  frontend:
    build:
      context: ./frontend/
    ports:
      - "5173:5173"
  backend:
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_URLS=http://+:8080